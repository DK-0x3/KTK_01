<Page x:Class="KTK.Pages.studentTeacher"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:KTK.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="400" d:DesignWidth="800"
      Title="studentTeacher">
    <Grid>
        <ComboBox HorizontalAlignment="Center" Width="250"/>
        <DataGrid x:Name="infoUserForDataGrid" Margin="0,50,0,0" ItemsSource="{Binding Users}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ФИО Ученика" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Контакты" Binding="{Binding Email}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>