<Page x:Class="KTK.Pages.scheduleStudent"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.modernwpf.com/2019"
      xmlns:local="clr-namespace:KTK"
      xmlns:controls="clr-namespace:KTK.controls"
      mc:Ignorable="d" 
      d:DesignHeight="400" d:DesignWidth="800"
      Title="scheduleStudent">

    <Grid>
        <ComboBox VerticalAlignment="Top" HorizontalAlignment="Center"
                  Width="250" Margin="0,10,0,0"/>
        <ScrollViewer Margin="10,50,10,10" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="scheduleWrapPanel" Orientation="Horizontal"
                       HorizontalAlignment="Center">
                
            </WrapPanel>
        </ScrollViewer>
        <!--<ui:SimpleStackPanel x:Name="scheduleStackPanel" Orientation="Horizontal" 
                             Margin="10,50,10,10" Spacing="30"/>-->
    </Grid>
</Page>
